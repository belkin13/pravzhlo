<?php

/**
 * @file
 * SUBTHEME theme file.
 */
/**
 * Implements hook_preprocess_HOOK()
 */
function pravzhlobin_preprocess_block(&$vars) {
    dump($vars['derivative_plugin_id']);
    if($vars['derivative_plugin_id'] == 'block-faqunansweredquestion') {
      
      $vars['#cache']['max-age'] = 0;
    }
  }